package ProgramaPingPong;

public class Semaforo {
    Semaforo semaforo = new Semaforo(1);
    Semaforo semaforo2 = new Semaforo(1);

    public Semaforo(int i) {
        semaforo = new Semaforo(1);
    }
}
